import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['https://118.195.146.26', 'https://192.168.1.45', 'https://localhost', 'http://localhost'],
};

export default nextConfig;
